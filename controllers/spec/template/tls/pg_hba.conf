# TYPE    DATABASE        USER            ADDRESS                 METHOD
# Replication connections by a user with the replication privilege
hostssl   replication     replication     all                     md5 clientcert=verify-ca
# As long as it is authenticated, all connections allowed except from non-encrypted connections
local     all             all                                     md5
hostssl   all             all             all                     md5 clientcert=verify-ca
hostnossl all             all             all                     reject
