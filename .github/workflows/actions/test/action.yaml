name: test
description: Run kubegres tests in Kind cluster
inputs:
  label:
    description: The label to filter the tests
    required: true
runs:
  using: composite
  steps:
    - name: Run tests
      env:
        TEST_LABEL: ${{ inputs.label }}
      run: make test
